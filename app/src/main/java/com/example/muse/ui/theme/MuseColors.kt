package com.example.muse.ui.theme

import androidx.compose.ui.graphics.Color

object MuseColors {
    // Elegant Dark Base Surfaces
    val Background = Color(0xFF0A0502)
    val SurfaceDark = Color(0xFF130905)
    val CardSurface = Color(0xFF1A100B)
    val CardBorder = Color(0xFF2C1910)
    val CardHighlight = Color(0xFF24150D)

    // Radiant Ember & Warm Accents
    val PrimaryAccent = Color(0xFFFF5014)
    val PrimaryGlow = Color(0x4DFF5014)
    val SecondaryAccent = Color(0xFF8B3010)
    val AccentLight = Color(0xFFFF7A45)
    val AccentGlow = Color(0x338B3010)

    // Legacy aliases mapped to Elegant Dark palette
    val CyanAccent = Color(0xFFFF5014) // Remapped to Elegant Dark signature accent
    val CyanGlow = Color(0x4DFF5014)
    val VioletAccent = Color(0xFF8B3010) // Remapped to deep terracotta
    val VioletGlow = Color(0x338B3010)
    val AmberDolby = Color(0xFFFF5014) // Remapped to radiant ember
    val EmeraldGreen = Color(0xFF22C55E)
    val RosePink = Color(0xFFFF453A)

    // Typography & Text
    val TextPrimary = Color(0xFFFFFFFF)
    val TextSecondary = Color(0xFF8E9299)
    val TextWarmIvory = Color(0xFFE0D8D0)
    val TextMuted = Color(0xFF5F5B57)

    val GlassBackground = Color(0xDD130905)
}

